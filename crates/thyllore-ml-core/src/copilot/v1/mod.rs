//! Curve-copilot v1 (rawfuture) — everything that is tied to the v1 model's
//! input/output contract: deploy-safe normalization, the forecast pipeline, and
//! the inference dump. A future model revision gets its own sibling module.

pub mod dump;
pub mod forecast;
pub mod rawfuture;
