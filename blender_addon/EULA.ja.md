# Thyllore Animation -- エンドユーザーライセンス契約 (EULA)

本 EULA は、Thyllore Animation に同梱される **プロプライエタリな ONNX
モデル** (以下「本コンポーネント」) に適用されます。具体的には以下です。

- 同梱の Curve Copilot ONNX モデル `models/curve_copilot.onnx`

本コンポーネントはプロプライエタリなソフトウェア (Copyright © 2026
kodai731) であり、販売ではなく本 EULA の条件の下でライセンスされます。
本コンポーネントは、ライセンス上問題のないデータ — 商用利用が許諾された
公開モーションキャプチャデータセット、および第 3 条に定める匿名化された
オプトインフィードバックデータ — のみで訓練されています。

本配布物に含まれる Python ソースファイル (`*.py`) は、**別途**
GPL-3.0-or-later の下でライセンスされます。GPL の条項は `LICENSE.md` を
参照してください。`wheels/thyllore_ml_core-*.whl` に含まれるコンパイル済み
Python 拡張 `thyllore_ml_core` は、その公開ソースリポジトリと同じく
Apache-2.0 の下でライセンスされます。`wheels/` に同梱されるサードパーティ
wheel はそれぞれのアップストリームライセンスに従います。帰属表示は
`THIRD_PARTY_LICENSES.md` を参照してください。

## 1. ライセンスの許諾

お客様 (以下「ライセンシー」) が本 EULA を承諾し遵守することを条件として、
kodai731 (以下「ライセンサー」) はライセンシーに対し、非独占的、譲渡不能、
全世界的かつ取消可能なライセンスを許諾します。その内容は以下のとおりです。

a. Blender 内で Thyllore Animation アドオンを実行する目的で、ライセンシー
   個人が所有する、またはライセンシーの雇用主が所有するマシンに本コンポー
   ネントをインストールし、使用すること。
b. 本コンポーネントが生成した出力 (生成された FCurve およびキーフレーム)
   を、個人・フリーランス・商業プロジェクトで使用すること。販売または
   頒布するプロジェクトを含みます。

ライセンシーは、生成した出力の著作権を完全に保持します。ライセンサーは、
ライセンシーが制作したアニメーションその他の成果物に対して、いかなる権利
も主張しません。

## 2. 制限事項

以下の行為は **禁止** されます。

a. 本コンポーネントの再頒布 -- `.onnx` モデルファイル自体、およびそれを
   含むアドオン ZIP のいずれについても、無償・有償を問わず、第三者への
   再頒布を禁止します。購入トークンやアドオン ZIP を非購入者と共有する
   ことは禁止されています。
b. 本コンポーネントのモデル重み、アーキテクチャ、または学習手順のリバース
   エンジニアリング、抽出、または再構築の試み。ただし、適用法がかかる制限
   を明示的に禁じる範囲についてはこの限りではありません。
c. 本コンポーネントの出力を、Blender その他の 3D コンテンツ制作ツールを
   対象とする競合 AI サービス、アニメーションアドオン、またはモデルの学習
   データや評価セットとして使用すること。
d. 本コンポーネントに含まれる著作権表示、ライセンス表示、その他の権利表示
   を除去、改変、または隠蔽すること。
e. 本コンポーネントを第三者にサブライセンス、賃貸、リース、または貸与する
   こと。

## 3. データ送信

a. ライセンシーがアドオンの設定 (プリファレンス) で有効にした場合に限り、
   アドオンは**匿名化された利用データ** (機能への入力、機能の出力、および
   ライセンシーによる補正内容に、アドオンのバージョンとランダムな匿名
   クライアント id を加えたもの) をライセンサーのエンドポイントに送信
   します。送信データにはオブジェクト名、ファイルパス、その他の個人情報
   は含まれず、元の作業内容を特定できない形に変換されます。
b. 送信には、上記の設定に加えて Blender の「オンラインアクセスを許可」
   設定が必要です。いずれかを無効にすると、すべての送信は直ちに停止
   します。送信を無効にしても、アドオンの基本機能は引き続き使用できます。
c. 任意の「Send Feedback」ボタンは、ライセンシーが入力した自由記述テキスト、
   アドオンのバージョン、および同一の匿名クライアント id のみを送信します。
d. ライセンサーは、送信されたデータを A.I. モデルおよびアドオンの改善のみ
   に使用し、第三者への販売、貸与、共有は行いません。

## 4. 個人情報

ライセンサーは個人情報を収集しません。購入時に提供される個人情報 (メール
アドレス等) は、ストアフロントがそのプライバシーポリシーに基づき収集・
処理します。ライセンス認証で送信されるのはライセンスキーと自動生成された
Device ID のみであり、個人情報は含まれません。

## 5. 保証の否認

本コンポーネントは **「現状有姿」** で提供され、商品性、特定目的への適合性、
および非侵害の保証を含むがこれらに限られない、明示または黙示のいかなる種類
の保証もありません。ライセンサーは、本コンポーネントが中断なくまたはエラー
なく動作すること、あるいは出力が特定の品質水準を満たすことを保証しません。

## 6. 責任の制限

適用法が許容する最大限の範囲において、ライセンサーは、契約、不法行為、
過失、厳格責任その他の法理に基づくかを問わず、本コンポーネントの使用
または使用不能に起因または関連して生じる、間接損害、付随的損害、特別損害、
結果的損害、懲罰的損害、または利益、収益、データ、事業機会の喪失について、
いかなる場合も責任を負いません。

本 EULA または本コンポーネントの使用に起因するライセンサーの累積責任の
総額は、責任の原因となった事象に先立つ 12 か月間にライセンシーが本コン
ポーネントに対して実際に支払った金額を超えないものとします。

## 7. 準拠法および管轄

本 EULA は、抵触法の原則を適用することなく、日本法に準拠します。本 EULA
に起因する一切の紛争は、東京地方裁判所を第一審の専属的合意管轄裁判所と
します。

## 8. 契約の終了

ライセンシーが第 2 条 (制限事項) に重大に違反した場合、本 EULA は通知
なく自動的に終了します。終了時、ライセンシーは
本コンポーネントの使用を中止し、自己の管理下にあるすべてのマシンから
削除しなければなりません。第 2 条の制限、第 5 条から第 6 条の免責、
および第 7 条は、終了後も存続します。

## 9. 返金

返金の可否は、購入したストアフロントが定めます。ライセンサーはストア
フロントが公表する返金ポリシーに従い、追加の制限を課しません。

## 10. サポート

サポートは、Thyllore Animation の公開 issue トラッカーを通じてベスト
エフォートで提供されます。サービスレベル契約 (SLA) は適用されません。

## 11. 完全合意および分離可能性

本 EULA は、`LICENSE.md` (GPL 部分を対象) および `THIRD_PARTY_LICENSES.md`
(同梱サードパーティパッケージを対象) と併せて、本コンポーネントに関する
ライセンシーとライセンサーの間の完全な合意を構成し、同一の主題に関する
事前または同時のすべての合意に優先します。

本 EULA のいずれかの条項が執行不能と判断された場合でも、残りの条項は
引き続き完全な効力を有します。

## 12. 連絡先

kodai731 <kodai731@gmail.com>

---

*最終更新: 2026-07-17。*

*本書は英語版 `EULA.md` の参考訳です。英語版と本日本語版の間に相違がある
場合は、英語版が優先します。*
