# Phase 3: blender_addon is consumed only as a Python package (grpc_client subpackage).
# Phase 4 will replace this file with bl_info + register / unregister entry points
# for Blender extension distribution.
